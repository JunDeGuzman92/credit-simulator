import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export const metadata: Metadata = {
  title: "Credit Score Simulator | ML-Powered Credit Risk Assessment",
  description: "Educational credit score simulator using LightGBM and FastAPI. Predict credit risk with machine learning based on the UCI German Credit dataset.",
  keywords: ["credit score", "machine learning", "credit risk", "FastAPI", "LightGBM", "Next.js"],
  authors: [{ name: "Jun De Guzman" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}